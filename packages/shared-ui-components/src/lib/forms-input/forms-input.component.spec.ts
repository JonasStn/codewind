import { FormsInputComponent } from './forms-input.component';

describe('FormsInputComponent', () => {
  let testUnit: FormsInputComponent;

  beforeEach(() => {
    testUnit = new FormsInputComponent();
  });

  it('should create', () => {
    expect(testUnit).toBeTruthy();
  });
});
