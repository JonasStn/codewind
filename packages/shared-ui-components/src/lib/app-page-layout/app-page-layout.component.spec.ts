import { AppPageLayoutComponent } from './app-page-layout.component';

describe('AppPageLayoutComponent', () => {
  let testUnit: AppPageLayoutComponent;

  beforeEach(() => {
    testUnit = new AppPageLayoutComponent();
  });

  it('should create', () => {
    expect(testUnit).toBeTruthy();
  });
});
