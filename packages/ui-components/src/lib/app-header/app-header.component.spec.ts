import { AppHeaderComponent } from './app-header.component';

describe('AppHeaderComponent', () => {
  let testUnit: AppHeaderComponent;

  beforeEach(() => {
    testUnit = new AppHeaderComponent();
  });

  it('should create', () => {
    expect(testUnit).toBeTruthy();
  });
});
