import { UiArticlesComponent } from './ui-articles.component';

describe('UiArticlesComponent', () => {
  let testUnit: UiArticlesComponent;

  beforeEach(() => {
    testUnit = new UiArticlesComponent();
  });

  it('should create', () => {
    expect(testUnit).toBeTruthy();
  });
});
